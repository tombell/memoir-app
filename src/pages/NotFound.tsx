import { FunctionalComponent } from "preact";

import Subheader from "$components/Subheader";

function NotFoundPage() {
  return <Subheader text="Page Not Found" />;
}

export default NotFoundPage as FunctionalComponent;
